import React from 'react'

const Button = ({name}) => {
  return (
    <div>
      <button className="px-5 m-2 bg-gray-200  border-spacing-0 rounded-md">{name}</button>
    </div>
  )
}

export default Button
